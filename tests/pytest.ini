[pytest]
addopts = -ra --strict-markers --strict-config
asyncio_mode = auto
testpaths = backend
filterwarnings =
    ignore:'asyncio.iscoroutinefunction' is deprecated:DeprecationWarning
markers =
    unit: deterministic isolated unit test
    api: FastAPI request/response contract test
    regression: regression test for a previously observed defect
